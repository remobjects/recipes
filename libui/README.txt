https://github.com/andlabs/libui is a simple and portable (but not inflexible) GUI library in C that uses the native GUI technologies of each platform it supports. 

Grab a release from https://github.com/andlabs/libui/releases to get the dlls.

The import project will import both 32 and 64bits versions.
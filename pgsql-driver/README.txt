This is a driver that binds the pgsql api to the Island DB api. This lets you use PostgreSQLDbConnection to connect to a database and the related classes to query it.

connectionstring should be like hostaddr=127.0.0.1 port=5432 user=username password=password dbname=database
This is a driver that binds the mariadb client api (Mysql/mariadb client driver) to the Island DB api. This lets you use MariaDbConnection to connect to a database and the related classes to query it.

connectionstring should be like hostname=127.0.0.1 port=5432 user=username password=password dbname=dbname